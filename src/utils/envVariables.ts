export const { DATABASE_URL } = process.env;
export const { ACCESS_TOKEN_SECRET } = process.env;
export const { JWT_ACCESS_TOKEN_PRIVATE_KEY } = process.env;
export const { JWT_ACCESS_TOKEN_PUBLIC_KEY } = process.env;
export const { JWT_REFRESH_TOKEN_PRIVATE_KEY } = process.env;
export const { JWT_REFRESH_TOKEN_PUBLIC_KEY } = process.env;
